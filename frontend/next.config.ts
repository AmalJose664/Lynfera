import type { NextConfig } from "next";
/** @type {import('next').NextConfig} */
const nextConfig: NextConfig = {
	// async rewrites() {
	// 	return [
	// 		{
	// 			source: '/api/:path*',
	// 			destination: process.env.NEXT_PUBLIC_API_SERVER_ENDPOINT + '/:path*',
	// 		}
	// 	]
	// }
	// /* config options here */

};

export default nextConfig;
